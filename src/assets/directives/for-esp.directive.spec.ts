import { ForEspDirective } from './for-esp.directive';

describe('ForEspDirective', () => {
  it('should create an instance', () => {
    const directive = new ForEspDirective();
    expect(directive).toBeTruthy();
  });
});
